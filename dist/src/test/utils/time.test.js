import DeviceHandler from "../../utils/data/DeviceHandler";
import { describe } from 'mocha';
describe('TaxCalculator', function () {
    describe('test calculateTaxAmount(salesPrice, taxRate)', function () {
        it('should calculate the tax for an item', function () {
            // let xxxx = await DeviceHandler.getDeviceId();
            // expect(xxxx === "xxxx")
        });
    });
});
